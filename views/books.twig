<!DOCTYPE html>
<html>
    <head>

        <meta charset='utf-8'>

        <title>Add A Book: Chattanooga Library</title>
        <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap.min.css'>
        <link rel='stylesheet' href='css/styles.css'>
    </head>
    <body>
        <div class='container'>

            <div class='page-header'>
                <h1>Chattanooga<small>Library</small></h1>
            </div>
            <p>Add a Book to the Library</p>
            <form action="/books" method="post">
                <label for='title'>Title</label>
                <input id='title' name='title' type='text'>
                <label for='genre'>Genre</label>
                <input id='genre' name='genre' type='text'>
                <label for='author'>Author</label>
                <input id='author' name='author' type='text'>

            <p><a href="/books"><button type='submit'>Add</button></a>
            </form>

            {% if books is not empty %}
            <p>Here are the books in the library.</p>
                {% for book in books %}
            <ul>
                <a href='/books/{{ book.getId }}'>
                <li>{{ book.getTitle }}</li>
                <p>{{ book.getGenre }}</p>
                </a>
            </ul>
                {% endfor %}
            {% endif %}
            <form action='/delete_all_books' method='post'>
            <p><button type='submit' method='post'>Delete All Books</button></p>


            <div class='page-footer'>

            </div>
        </div><!-- END DIV CONTAINER -->
    </body>
</html>
